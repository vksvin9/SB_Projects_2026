spring:
  application:
    name: template

  datasource:
    url: jdbc:oracle:thin:@localhost:1521/FREEPDB1
    username: app_user
    password: app_pass
    driver-class-name: oracle.jdbc.OracleDriver

  jpa:
    database-platform: org.hibernate.dialect.OracleDialect
    hibernate:
      ddl-auto: none
    show-sql: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

  mail:
    host: smtp.gmail.com
    port: 587
    username: vksvin9@gmail.com
    password: rsxeuiktqbngmnqw
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true
      mail.smtp.starttls.required: true

# Twilio (dev/testing)

# MSG91 (optional, not used now)
msg91:
  auth:
    key: dummy_key
  sender: VINAPP
  template:
    id: dummy_flow_id

# App flags
app:
  sms:
    provider: twilio
    dev-mode: false